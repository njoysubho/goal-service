micronaut:
  application:
    name: goal-service
  router:
    static-resources:
      swagger:
        paths: classpath:META-INF/swagger
        mapping: /swagger/**
flyway:
  datasources:
    default:
      locations: classpath:dbmigrations